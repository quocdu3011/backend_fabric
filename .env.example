# Fabric Network Configuration
PEER_ENDPOINT=peer0.org1.example.com:7051
MSP_ID=Org1MSP
CHANNEL_NAME=degree-channel
CHAINCODE_NAME=degree-cc

# Certificate Paths
CERT_PATH=/path/to/organizations/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/signcerts/cert.pem
KEY_PATH=/path/to/organizations/peerOrganizations/org1.example.com/users/User1@org1.example.com/msp/keystore/priv_sk
TLS_CERT_PATH=/path/to/organizations/peerOrganizations/org1.example.com/peers/peer0.org1.example.com/tls/ca.crt

# Server Configuration
PORT=3000
NODE_ENV=development

# Logging
LOG_LEVEL=info

# Authentication Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY=24h

# Rate Limiting Configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_ATTEMPTS=5

# Password Hashing
BCRYPT_SALT_ROUNDS=10

# Wallet Configuration
WALLET_PATH=./wallet

# PostgreSQL Database Configuration (Neon Cloud)
# Option 1: Use connection string (recommended for cloud databases)
DATABASE_URL=postgresql://neondb_owner:{Password}@ep-late-resonance-a1lkm30w-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# Option 2: Individual parameters (optional, only if DATABASE_URL not set)
DB_HOST=ep-late-resonance-a1lkm30w-pooler.ap-southeast-1.aws.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD={Password}

# Connection Pool Settings
DB_POOL_MAX=20
DB_POOL_MIN=2
DB_POOL_IDLE=10000
DB_POOL_ACQUIRE=30000
DB_POOL_EVICT=1000

# SSL Settings (required for Neon)
DB_SSL=true
DB_SSL_REJECT_UNAUTHORIZED=true

# Timeout Settings
DB_QUERY_TIMEOUT=30000
DB_STATEMENT_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000
DB_IDLE_TIMEOUT=30000
JWT_EXPIRY=24h
BCRYPT_SALT_ROUNDS=10
WALLET_PATH=./wallet

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_ATTEMPTS=100

# Default Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=adminpw
